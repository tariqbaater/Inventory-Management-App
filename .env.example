MYSQL_HOST=inventory-management-dukandb-i41rfq   # Dokploy internal service host
MYSQL_PORT=3306
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_DATABASE=dukan
PORT=8080
SESSION_SECRET=          # Required — random string for signing session cookies
CORS_ORIGIN=             # Optional — allowed origin (e.g. https://yourdomain.com), defaults to request origin
NODE_ENV=production      # Set to "production" for secure cookies
