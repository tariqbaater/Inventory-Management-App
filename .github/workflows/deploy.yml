name: Deploy to Dokploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code
        run: |
          cd /app
          git pull origin main

      - name: Restart app container
        run: |
          echo "Code pushed - Dokploy handles deployment"
