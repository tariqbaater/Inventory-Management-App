name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Trigger Dokploy Deployment
        run: |
          curl -X POST "https://admin.tariqbaater.com/api/deploy/uMHEdXyOn3h9g2AW-DTrJ" \
            -H "Content-Type: application/json"
